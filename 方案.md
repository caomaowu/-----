
## 总体方案：自动化PPT模流报告生成器
您的目标是：将一系列已命名好的Flow3D结果视频，自动填充到固定模板的PPT中，直接生成最终报告。
核心逻辑是：用程序（自动化脚本）充当"实习生"，完成所有重复、机械的填充工作，而您作为专家，专注于撰写结论。
视频命名与位置等信息在视频命名与位置、坐标.md文档中。C:\Users\gcb\Desktop\自动化方案\视频命名与位置、坐标.md
## 环境与工具
- **操作系统**：Windows
- **办公软件**：WPS PPT
- **核心技术**：OpenCV提取视频最后一帧，COM自动化插入并嵌入视频，锚点形状定位

## 核心功能要求
1. **视频完整嵌入**：必须将完整视频文件嵌入PPT，不可使用外部链接
2. **精确位置控制**：使用模板锚点形状固定视频和截图位置
3. **模板化设计**：支持不同报告模板的扩展
4. **自动化流程**：一键生成完整报告，无需中间审阅环节

## 详细技术方案

### 1. 文件命名规范
制定统一的文件命名规则，确保程序能自动识别：
```
视频命名与位置等信息在视频命名与位置、坐标.md文档中。C:\Users\gcb\Desktop\自动化方案\视频命名与位置、坐标.md

```

### 2. 模板锚点形状约定
在PPT模板中，为每个需要填充的视频/截图位置预先放置“锚点形状”（矩形占位框或现有框），并给形状命名。
- **命名规则**：`VID_<key>` 表示视频锚点；`IMG_<key>` 表示截图锚点（最后一帧）
- **定位方式**：脚本读取锚点形状的 `Left/Top/Width/Height`，将新插入的视频/图片设置为相同的位置和尺寸
- **模板可扩展性**：模板改版时只移动锚点形状，不改代码逻辑

### 3. 核心处理流程
1. **扫描与匹配**：扫描视频目录，根据命名规则匹配配置文件中的页面位置
2. **批量提取帧**：使用OpenCV批量提取所有视频的最后一帧，保存为临时图片
3. **视频嵌入**：通过COM自动化将完整视频文件插入到指定页，并放置到对应 `VID_<key>` 锚点形状位置
4. **图片插入**：在下一页插入对应的最后一帧图片
5. **清理临时文件**：删除提取的临时图片文件
6. **生成最终报告**：直接生成可用的最终PPT报告

### 4. WPS兼容性处理
- **技术方案**：使用WPS/PowerPoint的COM自动化接口操作PPT，确保“插入视频”为真实嵌入
- **视频嵌入**：生成的 `pptx` 拷贝到另一台电脑（无原视频文件）也能播放
- **格式验证**：检查生成的 `pptx` 解压后包含 `ppt/media/` 且媒体文件大小正常，并在WPS中抽检播放

### 5. 错误处理机制
- **文件缺失处理**：当视频文件缺失时，在对应位置插入占位符并记录日志
- **格式不匹配处理**：当文件格式不支持时，跳过该文件并记录错误
- **锚点缺失处理**：当模板中缺少对应锚点形状时，跳过该项并记录错误

### 6. 扩展功能设计
- **多模板支持**：通过配置文件切换不同的报告模板
- **批量处理**：支持一次性处理多个产品目录
- **日志记录**：详细记录处理过程和结果，便于问题排查

### 7. 质量保证措施
- **处理验证**：每个步骤完成后进行验证，确保视频和图片正确插入
- **文件完整性检查**：检查生成的PPT文件完整性
- **播放测试**：确保嵌入的视频在WPS中能正常播放

## 预期效果
- **效率提升**：将原来需要数小时的重复工作缩短到几分钟
- **质量保证**：通过标准化流程确保报告格式一致性
- **操作简化**：一键生成完整报告，您只需专注于撰写技术结论
- **可扩展性**：支持未来新增不同类型的报告模板
