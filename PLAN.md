# 自动化曲线数据提取方案实施计划 (CV-Curve-Parser)

## 1. 项目目标
实现**全自动、无人工干预**的坐标图曲线数据提取。系统应能自动识别仿真软件生成的坐标图，解析纵坐标刻度建立坐标系，追踪曲线轨迹并计算终点数值，最终自动替换 PPT 中的占位符。

## 2. 技术架构

### 2.1 核心流程
1.  **图像获取**: 从视频提取最后一帧或直接读取截图。
2.  **图表定位 (Auto-Detection)**:
    -   使用 OpenCV 轮廓检测识别画面中最大的白色矩形背景（通常为绘图区）。
3.  **坐标系建立 (Axis Calibration)**:
    -   **刻度识别**: 对纵轴区域进行 OCR 识别，提取刻度数值（如 `0.04`, `0.02`, `0`）。
    -   **智能纠错**: 利用刻度分布的线性特征，自动修正 OCR 识别错误（如将 `0.02` 误读为 `200` 或 `0 02`）。
    -   **线性拟合**: 建立 `像素 Y 坐标` -> `物理数值` 的线性映射关系。
4.  **曲线追踪 (Curve Tracking)**:
    -   **颜色分割**: 过滤背景色，提取有色曲线像素。
    -   **终点锁定**: 寻找曲线像素集中最右侧（Time End）的点 `(x, y)`。
5.  **数据回填**:
    -   **关联逻辑**: 根据占位符中的 `Key` 找到对应的视频截图。
    -   **数值计算**: 将终点 Y 坐标代入映射关系算出物理值。
    -   **PPT 替换**: 扫描文本框，替换 `{{CURVE:Key}}`。
    例子 : 如果视频锚点是 IMG_juanqi ，那么提取数据的占位符应写为 {{CURVE:juanqi}} 。这样程序就能精准找到 juanqi的截图进行分析

### 2.2 关键依赖
-   `opencv-python`: 图像处理、轮廓查找、颜色分割。
-   `pytesseract`: 坐标轴数字识别。
-   `numpy`: 数值计算与线性拟合。
-   **Tesseract-OCR (Portable)**: 集成在项目目录 `Tesseract-OCR/` 中，无需用户手动安装。

## 3. 实施步骤

### 阶段一：环境准备与原型验证
-   [ ] **依赖安装**: 更新 `requirements.txt`，添加 `pytesseract`。
-   [ ] **环境集成**: 在项目根目录建立 `Tesseract-OCR` 文件夹结构，并在代码中配置 `tesseract_cmd` 指向该本地路径。
-   [ ] **原型开发**: 编写 `test_curve_parser.py`，针对单张截图验证“坐标轴识别+线性纠错”与“曲线终点提取”的准确性。

### 阶段二：核心模块开发 (`curve_parser.py`)
-   [ ] **类设计**: 实现 `CurveParser` 类。
    -   `detect_chart_area()`: 智能裁剪图表区。
    -   `calibrate_y_axis()`: OCR 识别刻度并返回换算函数（包含 RANSAC 或线性回归去噪）。
    -   `get_curve_end_value()`: 综合计算终点物理值。
-   [ ] **容错处理**:
    -   处理 OCR 识别失败或误读的情况。
    -   处理多条曲线干扰（默认取最右侧数据）。

### 阶段三：PPT 生成器集成
-   [ ] **占位符解析**: 修改 PPT 处理逻辑，解析 `{{CURVE:VideoKey}}`。
    -   **逻辑**: 当遇到此占位符时，去查找名为 `VideoKey.mp4` (或模糊匹配) 对应的截图文件。
-   [ ] **流程整合**: 在插入视频截图后，调用 `CurveParser` 分析截图，并将结果填回当前 PPT 页面的对应文本框。

### 阶段四：测试与交付
-   [ ] **集成测试**: 使用真实视频/截图跑通全流程。
-   [ ] **用户验收**: 确认提取精度是否满足 `约 27g` 这种描述的精度要求。

## 4. 占位符规范
**(v2.2 新增：关联视频 Key)**

用户在 PPT 模板中需指定**视频的文件名 Key**，以便程序知道读哪张图：

-   **基础用法**: `{{CURVE:wendu}}`
    -   含义：读取 `wendu.mp4` (或 `VID_wendu` 对应) 的截图，分析曲线终值并填入。
-   **格式化**: `{{CURVE:wendu|.2f}}`
    -   含义：保留 2 位小数（例如 `27.35`）。
-   **指定颜色 (进阶)**: `{{CURVE:wendu|Red}}`
    -   含义：强制只提取红色曲线（用于一张图有多条线的场景）。

## 5. 待确认事项
-   [ ] **Tesseract 文件**: 需用户或我将 Tesseract 的免安装包文件放入 `自动化方案/Tesseract-OCR/` 目录下。
