# 自动化报告工具智能化改造方案

## 1. 现状分析
当前系统采用“配置驱动”模式，存在以下痛点：
- **硬编码依赖**：视频文件名与 PPT 锚点的对应关系必须在 `config.json` 中预定义。
- **扩展性差**：新增一种视频类型（如 `new_feature.mp4`）需要修改 PPT、修改配置文件、甚至可能需要调整代码。
- **维护成本高**：项目频繁变动时，手动同步配置文件容易出错。

## 2. 改造目标：完全去配置化 (Pure Convention-based Automation)
彻底废弃 `config.json` 中的 `video_mapping` 映射表，实现“**所见即所得**”的自动化：
- **无配置文件**：无需维护任何 JSON 映射，视频与 PPT 的关系由命名约定直接确定。
- **即插即用**：在 PPT 任意位置通过文本或形状命名标注 `VID_xxx`，只要文件夹有 `xxx.mp4`，即可自动填充。

## 3. 核心改造内容

### 3.1 智能化动态扫描 (`ppt_automation.py`)
- **全量提取**：自动提取 PPT 页面中所有符合 `VID_` (视频) 和 `IMG_` (图片) 前缀的形状。
- **多维度识别**：
    - **文本内容识别（首选）**：直接识别文本框或形状内的文字（如：`VID_wendu`）。这种方式最直观，用户操作最简单。
    - **形状名称识别（备选）**：识别“选择窗格”中的形状名称。
- **自动清理**：插入媒体后，自动删除占位的文本内容，确保报告美观。

### 3.2 自动化文件匹配逻辑 (`generate_report.py`)
- **约定优于配置**：
    1. 提取锚点 Key（如 `VID_wendu` -> `wendu`）。
    2. 直接在视频文件夹中搜索 `wendu.mp4`、`wendu.avi` 等。
- **容错处理**：如果文件夹中不存在对应视频，仅在日志中跳过并提醒，不报错，不影响其他页面生成。

### 3.3 `config.json` 的角色简化
- **彻底移除 `video_mapping` 模块**。
- `config.json` 仅保留全局 UI 标签等非业务逻辑配置。

### 3.4 智能尺寸自适应 (Smart Sizing)
为了兼顾“手动拉伸”的直观性和“自动生成”的便利性，系统将采用以下智能识别逻辑：

- **智能边界判定**：
    - **容器模式 (Auto-Fit)**：**无论形状类型**（文本框、矩形、占位符），只要用户手动拉伸了其尺寸（例如宽度和高度均大于 50 磅），程序将该区域视为“目标容器”，媒体将保持比例**缩放并居中填充**在其中。
    - **锚点模式 (Auto-Expand)**：如果形状非常小（如默认未拉伸的文本框），程序将其视为“插入中心坐标”。媒体将按**预设的黄金尺寸**（例如 PPT 宽度的 80%）自动展开，并以该点为中心对齐。
- **强制指令覆盖 (Directives)**：
    - 在文本内容中加入参数可实现**像素级控制**，无视形状物理尺寸。
    - 格式：`VID_key_W[宽度]_H[高度]_S[比例]`
    - 示例：`VID_wendu_W600`（锁定宽度 600，高度自适应）。
    - 示例：`VID_wendu_S1.0`（按视频原始 1:1 分辨率插入）。
- **比例锁定 (Aspect Ratio)**：系统始终强制锁定长宽比，无论用户把框拉成什么样，视频都不会变形，只会居中填充。
1. **修改 PPT**：在需要的位置画一个矩形，命名为 `VID_myvideo`。
2. **准备视频**：将 `myvideo.mp4` 放入视频文件夹。
3. **运行**：程序自动识别并插入。

## 5. 实施计划
1. **第一阶段**：重构 `ppt_automation.py` 的扫描逻辑，支持全量提取。
2. **第二阶段**：重构 `generate_report.py` 的主循环，从“遍历配置”改为“遍历 PPT 锚点”。
3. **第三阶段**：增强 `utils.py` 中的文件搜索辅助函数。
4. **第四阶段**：测试兼容性，确保旧版 `config.json` 依然有效。

---
**设计原则**：约定优于配置 (Convention over Configuration)
